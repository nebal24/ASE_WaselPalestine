package com.wasel.model;

public enum ModerationActionType {
    VERIFY,
    REJECT,
    MARK_DUPLICATE,
    RESET_TO_PENDING,
    AUTO_VERIFY,
    AUTO_REJECT,
    AUTO_MARK_DUPLICATE,
    VOTE_CAST,
    VOTE_CHANGED,
    VOTE_REMOVED
}