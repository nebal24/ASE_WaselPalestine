package com.wasel.controller;
public class package_info {
}

/*The controller layer is responsible for handling HTTP requests
and returning responses.

Responsibilities:
- Mapping API endpoints
- Receiving request data (DTOs)
- Calling service methods
- Returning structured responses

Controllers must not contain business logic or database access.
They act as an entry point to the system.*/
//-----------------------------------------------------------
/*تستقبل الطلب (GET / POST / PUT / DELETE)
تستقبل البيانات (JSON)
تستدعي Service
ترجع Response*/
